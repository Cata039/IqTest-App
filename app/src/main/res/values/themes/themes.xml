<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- Base theme with no ActionBar -->
    <style name="Theme.IQTestApp.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- your normal app colors -->
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorPrimary">@color/black</item>
        <!-- draw behind the status bar -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>


    <style name="Theme.IQTestApp" parent="Base.Theme.IQTestApp" />
</resources>
